# =========================================================
#  LLM 로컬 환경 — Git 무시 목록
#  민감 정보·대용량 파일·로그·캐시를 저장소에서 제외합니다.
# =========================================================

# ─────────────────────────────────────────────
#  설치된 환경 전체 (대용량 + 사용자별)
# ─────────────────────────────────────────────
# 14GB+ 의 모델 데이터, 컨테이너, venv 등
# 코드만 저장소에 두고 환경은 install.py 로 재생성
llm_environment/


# ─────────────────────────────────────────────
#  개인 설정 파일 (사용자별)
# ─────────────────────────────────────────────
# 마지막 워크스페이스 경로 등 개인 정보 포함
launcher/settings/user_config.json
launcher/settings/*.json
!launcher/settings/.gitkeep
.webui_secret_key


# ─────────────────────────────────────────────
#  Python
# ─────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so

# 가상환경 (격리된 venv 들)
venv/
.venv/
env/
.env/

# 배포 / 빌드
dist/
build/
*.egg-info/
*.egg
.eggs/

# pip / setuptools
pip-log.txt
pip-delete-this-directory.txt
*.whl


# ─────────────────────────────────────────────
#  로그 / 임시 / 캐시
# ─────────────────────────────────────────────
*.log
logs/
*.tmp
*.cache
.cache/
.pytest_cache/
.mypy_cache/
.ruff_cache/


# ─────────────────────────────────────────────
#  운영체제
# ─────────────────────────────────────────────
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Linux
*~
.Trash-*


# ─────────────────────────────────────────────
#  IDE / 에디터
# ─────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/


# ─────────────────────────────────────────────
#  환경변수 / 비밀
# ─────────────────────────────────────────────
.env
.env.local
.env.*.local
*.pem
*.key
secrets.json
secrets.yaml
credentials.json


# ─────────────────────────────────────────────
#  모델 / 데이터 파일 (대용량)
# ─────────────────────────────────────────────
# 어디에 있든 GGUF/safetensors 파일은 저장소에 안 들어감
*.gguf
*.safetensors
*.bin
*.pth
*.pt
*.ckpt
*.h5
*.onnx

# 모델 메타
manifests/
blobs/


# ─────────────────────────────────────────────
#  Docker (이미지·컨테이너는 시스템에 저장됨, 코드만 저장)
# ─────────────────────────────────────────────
# Dockerfile 자체는 저장 (재현 가능성)
# 빌드 컨텍스트 산출물은 제외
.docker/


# ─────────────────────────────────────────────
#  사용자 작업 데이터
# ─────────────────────────────────────────────
# 에이전트가 만든 파일 / 사용자 업로드
agent/workspace/*
!agent/workspace/.gitkeep
!agent/workspace/README.txt

# Open WebUI 채팅 기록·업로드 파일
chat_ui/data/
*.db
*.sqlite
*.sqlite3
